name: horizonte
description: "Horizonte — clareza financeira sem culpa."
publish_to: 'none'

version: 2.0.0+2

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8
  path_provider: ^2.1.5
  drift: ^2.34.3
  sqlite3_flutter_libs: ^0.6.0+eol
  path: ^1.9.1
  intl: ^0.20.2
  shared_preferences: ^2.5.3
  excel_plus: ^2.22.0
  file_picker: ^13.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  drift_dev: ^2.34.5
  build_runner: ^2.15.1

flutter:
  uses-material-design: true
