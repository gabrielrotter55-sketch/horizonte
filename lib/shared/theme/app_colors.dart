import 'package:flutter/material.dart';

class AppColors {
  // ==========================================
  // IDENTIDADE HORIZONTE
  // ==========================================

  /// Roxo principal.
  /// Usado para identidade, ações e destaques.
  static const primary = Color(0xFF6C4AB6);

  /// Roxo mais escuro para elementos de destaque.
  static const primaryDark = Color(0xFF563A91);

  /// Roxo bem claro para fundos selecionados.
  static const primaryLight = Color(0xFFF1ECFA);

  // ==========================================
  // CORES BASE
  // ==========================================

  /// Fundo principal do aplicativo.
  static const background = Color(0xFFF7F7F9);

  /// Branco utilizado nos cards.
  static const card = Colors.white;

  /// Grafite principal.
  static const textPrimary = Color(0xFF202124);

  /// Cinza para informações secundárias.
  static const textSecondary = Color(0xFF737373);

  /// Bordas e divisores discretos.
  static const border = Color(0xFFE5E5E8);

  // ==========================================
  // FINANCEIRO
  // ==========================================

  /// Crescimento, receitas e valores positivos.
  static const success = Color(0xFF2E8B57);

  /// Alertas e situações que exigem atenção.
  static const danger = Color(0xFFD64545);

  /// Informação neutra.
  static const info = Color(0xFF6C4AB6);
}